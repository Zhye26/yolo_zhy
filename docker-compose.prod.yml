version: '3.8'

services:
  web:
    environment:
      - STARTUP_STRICT=true
      - GUNICORN_BIND=0.0.0.0:5000
      - WEB_CONCURRENCY=2
      - GUNICORN_THREADS=2
      - GUNICORN_TIMEOUT=120
    command: gunicorn -c gunicorn.conf.py run:app
